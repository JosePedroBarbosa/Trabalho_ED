import game.menus.Menus;

/**
 * This class contains the main method that initializes the game by calling the
 * welcomeMenu() method to display the welcome menu and start the game.
 */
public class IMFSimulator {
    public static void main(String[] args) {
        Menus.welcomeMenu();
    }
}

